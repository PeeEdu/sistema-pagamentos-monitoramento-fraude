spring:
  data:
    mongodb:
      uri: mongodb://admin:admin123@localhost:27017/test_db?authSource=admin
    redis:
      host: localhost
      port: 6379
      password: redis123

  kafka:
    bootstrap-servers: localhost:9092

  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password: sa

camunda:
  bpm:
    enabled: false